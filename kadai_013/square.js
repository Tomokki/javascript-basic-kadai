function calculate(value) {
  const result = value * value;
  return result;
}

const inputValue = 10; // 引数の値を指定します
const squareResult = calculate(inputValue); // 関数を呼び出して計算結果を取得します
console.log(squareResult); // 計算結果をコンソールに出力します
